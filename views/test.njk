{% extends "testlayout.njk" %}
{% block  content %}
<main>
    <a id="top"></a>
    <h1>{{ title }}</h1>
    <p>{{ segment }}</p>
    <p>Detta är test.njk som används av /test router.</p>
    <ul>
        {% for row in rows %}
        <li>{{row.id}} {{ row.name }} <br> {{ row.secret_identity }} <br> {{row.bio}} <br><br></li>
        {% endfor %}
    </ul>
</main>
{% endblock %}%